APP_PORT=8000             # Port FastAPI will run on
CHROMA_PATH=/app/chroma   # Path for local Chroma DB index (used for embeddings)
DATABASE_URL=sqlite:///./study_buddy.db  # Example local DB URI (override in prod)
DEBUG_PORT=5678           # Port for attaching debugger (e.g. VSCode + debugpy)
ENV=development           # App environment flag
LLM_MODE=ollama           # AI model provider: ollama (local) or openai (cloud)
SECRET_KEY=dev-secret     # Used for signing sessions or JWTs (rotate in prod)

